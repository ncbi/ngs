
MAKE ?=     make
DIRS =      adapter dispatch language

all:
	for dir in ${DIRS}; do \
	    ${MAKE} -C $$dir -f Makefile.simple; \
	done

clean:
	for dir in ${DIRS}; do \
	    ${MAKE} -C $$dir -f Makefile.simple clean; \
	done

